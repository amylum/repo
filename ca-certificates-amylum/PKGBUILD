# Maintainer: Les Aker <me@lesaker.org>
pkgname=ca-certificates-amylum
_realname=ca-certificates
pkgver=20185_1_1
pkgrel=1
pkgdesc="Common CA certificates"
arch=('x86_64')
url="https://github.com/amylum/ca-certificates"
license=('MIT')
source=("https://github.com/amylum/${_realname}/releases/download/${pkgver//_/-}/${_realname}.tar.gz")
noextract=("${_realname}.tar.gz")
sha512sums=("3ea82882dda8b7608b0a444331094f21d55663058c15173a9cc803e422f01584eac0c7cd7812c21902176ff7ca845143fb06b3b8064ce6c66bf815362b26d125")

install=ca-certificates.install

depends=('bash-amylum' 'findutils-amylum' 'p11-kit-amylum')
conflicts=("${_realname}")
provides=("${_realname}" "ca-certificates-utils")

package() {
    cd "${pkgdir}"
    tar xf "${srcdir}/${_realname}.tar.gz"
    mv usr/share/licenses/${_realname} usr/share/licenses/${pkgname}
}

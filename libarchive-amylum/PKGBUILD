# Maintainer: Les Aker <me@lesaker.org>
pkgname=libarchive-amylum
_realname=libarchive
pkgver=3.2.0_2
pkgrel=1
pkgdesc="library that can create and read several streaming archive formats"
arch=('x86_64')
url="https://github.com/amylum/libarchive"
license=('BSD')
source=("https://github.com/amylum/${_realname}/releases/download/${pkgver//_/-}/${_realname}.tar.gz")
noextract=("${_realname}.tar.gz")
sha512sums=("2349b8a5c653e8d69ad047786efa65eb8eca569466827b7fd27fed10fc3a007fb6fde5cf5f9863a2997fe38626ed27fd2a0037030f89ac855de8c83e5484edaa")

conflicts=("${_realname}")
provides=("${_realname}")

package() {
    cd "${pkgdir}"
    tar xf "${srcdir}/${_realname}.tar.gz"
    mv usr/share/licenses/${_realname} usr/share/licenses/${pkgname}
}

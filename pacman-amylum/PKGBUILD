# Maintainer: Les Aker <me@lesaker.org>
pkgname=pacman-amylum
_realname=pacman
pkgver=5.1.0_1
pkgrel=1
pkgdesc="A library-based package manager with dependency support"
arch=('x86_64')
url="https://github.com/amylum/pacman"
license=('GPL2')
source=("https://github.com/amylum/${_realname}/releases/download/${pkgver//_/-}/${_realname}.tar.gz")
noextract=("${_realname}.tar.gz")
sha512sums=("f5dfdf4239fdcc1c9235cfb756a38aa1134ea432e4e885ef380d1bd53861abd6410f4356bbc8c9102dca7e5041e5fd02354f700d49383d6ca45543bcf3de0bcd")

backup=(etc/pacman.conf etc/makepkg.conf)

depends=('bash-amylum' 'gnupg-amylum' 'libarchive-amylum')

conflicts=("${_realname}")
provides=("${_realname}")

package() {
    cd "${pkgdir}"
    tar xf "${srcdir}/${_realname}.tar.gz"
    mv usr/share/licenses/${_realname} usr/share/licenses/${pkgname}
}

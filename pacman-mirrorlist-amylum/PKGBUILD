# Maintainer: Les Aker <me@lesaker.org>
pkgname=pacman-mirrorlist-amylum
_realname=pacman-mirrorlist
pkgver=20170514
pkgrel=1
pkgdesc="Arch Linux mirror list for use by pacman"
arch=('x86_64')
url="https://github.com/amylum/pacman-mirrorlist"
license=('MIT')
source=("https://github.com/amylum/${_realname}/releases/download/${pkgver//_/-}/${_realname}.tar.gz")
noextract=("${_realname}.tar.gz")
sha512sums=("11e040581a4b2b85f99bb89ad572fa863d76978a5559db91f76bcef5d0d8fbcdd95387eaee8be9a4ff80b63dc2d2726fc63a9c67e5114f41ca030ec869b731ae")

backup=(etc/pacman.d/mirrorlist)

conflicts=("${_realname}")
provides=("${_realname}")

package() {
    cd "${pkgdir}"
    tar xf "${srcdir}/${_realname}.tar.gz"
}

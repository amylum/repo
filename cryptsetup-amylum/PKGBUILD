# Maintainer: Les Aker <me@lesaker.org>
pkgname=cryptsetup-amylum
_realname=cryptsetup
pkgver=1.7.5_23
pkgrel=1
pkgdesc="Userspace setup tool for transparent encryption of block devices using dm-crypt"
arch=('x86_64')
url="https://github.com/amylum/cryptsetup"
license=('GPL3')
source=("https://github.com/amylum/${_realname}/releases/download/${pkgver//_/-}/${_realname}.tar.gz")
noextract=("${_realname}.tar.gz")
sha512sums=("aad813105350cfe49df2dbdf015870eab775e11395ee67ae2698fc94f9fe0c5ea7a6708b187442609e6b76924a43cec28ef21501291cd79273662e802acecd05")

conflicts=("${_realname}")
provides=("${_realname}")

package() {
    cd "${pkgdir}"
    tar xf "${srcdir}/${_realname}.tar.gz"
    mv usr/share/licenses/${_realname} usr/share/licenses/${pkgname}
}

machine:
  services:
  - docker
  environment:
    PACMAN_VERSION: v5.0.2
dependencies:
  override:
  - 'echo "Prospectus: $GITHUB_CREDS" >> .octoauth.yml'
test:
  override:
  - make docker-build
deployment:
  release:
    branch: master
    commands:
    - make docker-upload
